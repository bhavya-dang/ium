/// <reference types="node" />
import EventEmitter = require('events');
import { Playlist as Cassette } from 'cassette';
import { StreamOptions, VoiceChannel, VoiceConnection } from 'discord.js';
import GuildExtension from './GuildExtension';
export declare type EndReason = 'temp' | 'terminal';
export default class Playlist extends Cassette {
    static ensureVoiceConnection(channel: VoiceChannel): Promise<VoiceConnection>;
    readonly events: EventEmitter;
    readonly guild: GuildExtension;
    private _playing;
    constructor(guild: GuildExtension);
    private readonly _dispatcher;
    readonly playing: boolean;
    stop(): void;
    destroy(): void;
    pause(): void;
    resume(): void;
    start(channel: VoiceChannel, options?: StreamOptions): Promise<void>;
    private _start(options?);
    private _end(reason?);
    private _destroy();
}
